test_that("unlock wb works", {

  # test_file <- "locked_wb_test.xlsx"
  # password <- "123"
  # src_dir <- system.file("extdata", package = "excelsioR")
  # file_dir_df <- data.frame(file_path = src_dir, file_name = test_file)
  #
  # test_wb <- unlock_wb(file_dir_df, wb_password = password)
  #
  # test_df <- read_wb(src_dir)
  # expected_df <- data.frame(test, wb, unlocked)
  #
  # expect_equal(test_df, expected_df)
})
